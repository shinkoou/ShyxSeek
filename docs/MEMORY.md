# Memory

Memory is classified by `MemoryType`. Sensitive memory is never created automatically. v0.1 exposes explicit manual saving. Embeddings are local deterministic hashing vectors so no private text leaves the device for indexing.
